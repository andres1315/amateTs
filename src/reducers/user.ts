/* export const userInitialState = []

export const reducer = (state, action) => {
  const { type, payload } = action
  switch (type) {
    case 'SET_USER': {
      const { user } = payload
      const userSaveLocalStorage = JSON.stringify(user)
      localStorage.setItem('user', userSaveLocalStorage)

    }
  }

  return state
} */
